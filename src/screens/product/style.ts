import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  form: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',

    height: '100%',
    width: '100%'
  },
  formTitle: {
    fontSize: 32,
    fontWeight: 'bold'
  }
});
